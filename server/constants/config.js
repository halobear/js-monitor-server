const config = {
  secretKey: 'halo_monitor',
  mysql: {
    host: 'localhost',
    port: '3306',
    user: 'root',
    password: '123456',
    database: 'halo_monitor',
  },
  port: 9601,
}

module.exports = config
